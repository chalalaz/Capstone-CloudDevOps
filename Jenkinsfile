pipeline {
  agent any
  stages {
    stage('Linting') {
      steps {
        sh 'echo "Hello World"'
      }
    }
    stage('Build image') {
    }
    stage('Push image') {
    }
    stage('set current kubectl context') {
    }
    stage('Deploy container') {
    }
  }
}
