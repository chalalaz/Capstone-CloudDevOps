pipeline {
  agent any
  stages {
    stage('Build') {
      stage('Linting') {
        sh 'echo "Hello World"'
      }
      stage('Build image') {
      }
      stage('Push image') {
      }
      stage('set current kubectl context') {
      }
      stage('Deploy container') {
      }
    }
  }
}
